@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome!</h1>
    @Html.ActionLink("Add quiz", "Add", "Quiz", null,new {@class="btn btn-primary"})

</div>
